{% assign graph_full = image_full | default: image %}
{% assign graph_half_horizontal = image_half_horizontal | default: image %}
{% assign graph_half_vertical = image_half_vertical | default: image %}
{% assign graph_quadrant = image_quadrant | default: image %}

{% assign weather_title = title | default: "Météo-France observations" %}
{% assign weather_title_compact = title_compact | default: weather_title %}